spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      authentication-database: admin
      database: movies-list
      username: mongodb
      password: password
  cache:
    type: simple

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG

eureka:
  client:
    enabled: false