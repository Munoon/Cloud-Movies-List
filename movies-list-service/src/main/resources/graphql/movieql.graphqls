type Movie {
    id: ID!,
    name: String
}

input CreateMovie {
    name: String
}

type Query {
    movie(id: ID): Movie
    latestMovies(count: Int, page: Int): [Movie]
}

type Mutation {
    addMovie(newMovie: CreateMovie!): Movie
}