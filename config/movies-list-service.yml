security:
  oauth2:
    resource:
      jwt:
        keyValue: ${jwt.keyValue}

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      authentication-database: admin
      database: movies-list
      username: mongodb
      password: password