spring:
  application:
    name: gateway-service

zuul:
  sensitive-headers: Cookie,Set-Cookie
  routes:
    users:
      path: /users/**
      serviceId: user-resource-service

logging:
  level:
    org:
      springframework:
        security:
          web:
            csrf:
              CsrfFilter: debug